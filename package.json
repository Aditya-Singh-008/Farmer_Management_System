{
  "name": "farmer-management-system",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "vercel-build": "cd frontend && npm install --no-audit --no-fund && npm run build"
  }
}
